import React from "react";
import PlayerName from "./PlayerName";

const NewPlayer = props => {

    return (
        <div className="rank-input-new-container">
            <PlayerName />
        </div>
      );
};

export default NewPlayer;